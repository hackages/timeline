import React from 'react';
import Events from './events';

const Section = () => {
  return (<section id="cd-timeline" className="cd-container"></section>);
};

const Timeline = () => {
  return (
     <Events/>
  );
};


export default Timeline;
